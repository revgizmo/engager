% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/name_matching_workflow.R,
%   R/safe_name_matching_workflow.R
\name{safe_name_matching_workflow}
\alias{safe_name_matching_workflow}
\title{Safe name matching workflow (privacy-first)}
\usage{
safe_name_matching_workflow(
  transcript_file_path = NULL,
  roster_data = NULL,
  privacy_level = getOption("engager.privacy_level", "mask"),
  unmatched_names_action = getOption("engager.unmatched_names_action", "stop"),
  data_folder = ".",
  section_names_lookup_file = "section_names_lookup.csv"
)

safe_name_matching_workflow(
  transcript_file_path = NULL,
  roster_data = NULL,
  privacy_level = getOption("engager.privacy_level", "mask"),
  unmatched_names_action = getOption("engager.unmatched_names_action", "stop"),
  data_folder = ".",
  section_names_lookup_file = "section_names_lookup.csv"
)
}
\arguments{
\item{transcript_file_path}{Path to the transcript file to process}

\item{roster_data}{Data frame containing roster information}

\item{privacy_level}{Privacy level to apply. One of \code{c("ferpa_strict", "ferpa_standard", "mask", "none")}.
Defaults to \code{getOption("engager.privacy_level", "mask")}.}

\item{unmatched_names_action}{Action to take for unmatched names. One of \code{c("stop", "warn")}.
Defaults to \code{getOption("engager.unmatched_names_action", "stop")}.}

\item{data_folder}{Directory containing data files}

\item{section_names_lookup_file}{Name of the section names lookup file}

\item{transcripts_df}{Tibble/data.frame with at least a \code{speaker} column.}

\item{roster_df}{Tibble/data.frame returned by \code{load_roster()}.}

\item{options}{List of options; supports \code{match_strategy} (default \code{'exact'})
and \code{include_name_hash} (default \code{FALSE}).}
}
\value{
A list with elements \code{transcripts_with_ids}, \code{unresolved}, and
\code{audit}. Returns an object of class \code{'engager_match'} with redacted
\code{print()} and \code{summary()} methods.

Processed transcript data with privacy-aware name matching applied
}
\description{
Returns an object of class 'engager_match' with redacted print/summary.

Main workflow function for privacy-first name matching. Implements two-stage
processing: Stage 1 (unmasked matching in memory) and Stage 2 (privacy masking
for outputs). Provides configuration-driven behavior for unmatched names.
}
\examples{
# Default behavior (maximum privacy)
# result <- safe_name_matching_workflow(
#   transcript_file_path = "transcript.vtt",
#   roster_data = roster_df
# )
#
# Opt-in for convenience
# result <- safe_name_matching_workflow(
#   transcript_file_path = "transcript.vtt",
#   roster_data = roster_df,
#   unmatched_names_action = "warn"
# )
}
\seealso{
Other name-matching: 
\code{\link{detect_unmatched_names}()},
\code{\link{load_roster}()},
\code{\link{write_unresolved}()}
}
\concept{name-matching}
