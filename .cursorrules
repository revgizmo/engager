# Cursor adapter for engager

The authoritative agent instructions are in `AGENTS.md`; the development and
merge policy is in `CONTRIBUTING.md`. Read both before acting.

Use `project-docs/ROADMAP.md` and the relevant release plan for product scope.
Historical development documents and scripts are non-authoritative. Never use
real course data, make compliance claims, or treat admin bypass as routine.
